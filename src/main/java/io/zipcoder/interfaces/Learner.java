package io.zipcoder.interfaces;

//interfaces can only be public so don't need to specify
public interface Learner {

    //interfaces can only be public so don't need to specify
    void learn(Double numberOfHours);

    Double getTotalStudyTime();
}
